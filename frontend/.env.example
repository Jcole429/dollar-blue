# Base URL of the Python backend that serves the latest rates.
# Defaults to the deployed instance when unset, so this is only needed to point
# the frontend at a local backend.
NEXT_PUBLIC_API_URL=https://dollar-blue-backend.vercel.app
